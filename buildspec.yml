version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.9
    commands:
      - echo Installing...

  build:
    commands:
      - echo Running app...
      - python app.py

  post_build:
    commands:
      - echo Build completed!

artifacts:
  files:
    - '**/*'
